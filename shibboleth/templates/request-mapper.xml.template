<RequestMap xmlns="urn:mace:shibboleth:3.0:native:sp:config">
    <Host name="${SHIB_HOSTNAME}"
        authType="shibboleth"
        requireSession="true"
        exportAssertion="true"
        port="8443"
        scheme="https"
        redirectToSSL="8443">
    </Host>
</RequestMap>
